# Файл с историей сгенерированных паролей (ВНИМАНИЕ: содержит пароли в открытом виде!)
# Если вы не хотите, чтобы ваша история уходила на GitHub, раскомментируйте строку ниже:
password_history.json

# Кэш и служебные файлы Python
__pycache__/
*.pyc
venv/
env/
dist/
build/
*.spec

# Файлы настроек редакторов (PyCharm, VSCode)
.idea/
.vscode/

# Системные файлы macOS/Windows
.DS_Store
Thumbs.db